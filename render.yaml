services:
  - type: web
    name: woki-command-center-demo
    runtime: python
    plan: free
    region: virginia
    rootDir: lora-emergencia/center
    buildCommand: pip install -r requirements.txt
    startCommand: python center.py --demo --public-demo --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
